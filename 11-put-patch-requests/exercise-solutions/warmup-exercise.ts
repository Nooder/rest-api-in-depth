/*
    1. Create a new Deno project/workspace

    2. Respond to any PUT request with 200 status code
       (regardless of route)

    3. Respond to any PATCH request with 200 status code
       (regardless of route)

    4. Respond to all other types of requests with a
       404 status code

    5. Add a JSON body to your PUT and PATCH requests in
       Insomnia and log out the contents of the body in
       the console

    6. Test this in Insomnia and change the request
       method type to check for the 404s and 200s. Also,
       check to make sure the body is logged out too
*/
