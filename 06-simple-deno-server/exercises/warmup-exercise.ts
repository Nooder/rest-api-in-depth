/*
    1. Start a new Deno workspace and create a
       server.ts file

    2. Start a new Deno Server with Deno.serve
       on port 9000 and have it always return
       a String response "Hello!" to all Requests

    3. Start your server using deno run and
       Test this in both Insomnia and the Browser
*/
